/**
 * Fixture assets: what the mock providers return, and what a new graph
 * shows before anything has run. Served from public/fixtures.
 */
export const FIXTURES = {
  /** the default image for the image workflow — a black bear, pink snout, clear sky */
  blackBear: "/fixtures/black-bear.png",
} as const;
